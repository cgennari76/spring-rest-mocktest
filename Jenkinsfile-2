pipeline {
  agent any
  
  stages {
    stage('Hello') {
      agent {
        docker { 
	         image 'chrisgennari/dry-run-cloud:jenkins-ansible'
		 args '--entrypoint='
	       }
      }
      steps {
        sh "which ansible"
	sh "ansible --version"
      }
    }
  }
}
