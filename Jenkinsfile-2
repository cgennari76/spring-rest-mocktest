pipeline {
  agent any
  
  stages {
    stage('Hello') {
      agent {
        docker { image 'chriswayg/docker-alpine-ansible' }
      }
      steps {
        sh "ansible-galaxy  --version"
	sh "ansible-playbook --version"
      }
    }
  }
}
