pipeline {
  agent any
  
  stages {
    stage('Hello') {
      agent {
        docker { label 'ansible-docker-slave' }
      }
      steps {
        sh "ansible-galaxy  --version"
	sh "ansible-playbook --version"
      }
    }
  }
}
