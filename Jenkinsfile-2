pipeline {
  agent any
  
  stages {
    stage('Hello') {
      agent {
        docker { 
	         image 'chrisgennari/dry-run-cloud:jenkins-ansible'
		 args '--entrypoint='
	       }
      }
      steps {
        sh "python3 -m ansible-galaxy  --version"
	sh "python3 -m ansible-playbook --version"
      }
    }
  }
}
