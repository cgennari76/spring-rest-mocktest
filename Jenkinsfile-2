pipeline {
  agent none
  
  stages {
    stage('Hello') {
      agent {
        docker { 
	         image 'chrisgennari/dry-run-cloud:jenkins-ansible'
	       }
      }
      steps {
	sh 'ansible --version'
      }
    }
  }
}
