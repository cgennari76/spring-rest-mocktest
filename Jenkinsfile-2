pipeline {
  agent none
  
  stages {
    stage('Hello') {
      agent {
        docker { 
	         image 'maven:3.8.1-adoptopenjdk-11'
		 args '-u root'
	       }
      }
      steps {
        sh 'mvn --version'
      }
    }
  }
}
