pipeline {
  agent any
  
  stages {
    stage('Hello') {
      agent {
        docker { image 'chrisgennari/dry-run-cloud:ansible-docker-slave' }
      }
      steps {
        sh "ansible-galaxy  --version"
	sh "ansible-playbook --version"
      }
    }
  }
}
