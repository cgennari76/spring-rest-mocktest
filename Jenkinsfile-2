pipeline {
  agent any
  
  stages {
    stage('Hello') {
      agent {
        docker { image 'alpine/ansible' }
      }
      steps {
        sh "ansible-galaxy  --version"
	sh "ansible-playbook --version"
      }
    }
  }
}
