pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                sshagent(['jenkins_agent']) {
                    sh "ansible-playbook -i inventory.ini deploy.yml"
                }
            }
        }
    }
}
